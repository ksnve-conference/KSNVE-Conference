# KSNVE 학술대회 앱 진단서

검토 대상: ksnve-conference · 커밋 41ea5af · 검토일 2026-08-26
방법: 코드 리딩 + 375×812 뷰포트 실기동(라이트/다크) + 프로덕션 빌드 산출물 분석
데이터: 세션 68 · 논문 319 (npm run typecheck 통과, npm run validate:data 통과 — 경고 2건)

## 총평

엔지니어링은 상급, 정보 설계는 미완입니다. 데이터/코드 완전 분리, 오프라인 캐싱, 폰트 서브셋,
`.ics` 내보내기 + 시간 충돌 감지 등은 이미 잘 되어 있습니다. 문제는 화면이 같은 말을 두 번 하고,
멀티트랙 학술대회를 한 줄짜리 목록으로 표현하고 있으며, 초록 전문 244KB(gzip)가 공지사항 같은
화면에서도 함께 내려간다는 점입니다.

| 축 | 점수 |
|---|---|
| 코드 구조 | 5/6 |
| 오프라인·PWA | 5/6 |
| 성능·번들 | 2/6 |
| 정보 설계 | 2/6 |
| 시각 디자인 | 3/6 |
| 접근성 | 3/6 |

한 문장 요약 — "프로그램북을 잘 옮긴 앱"에서 "현장에서 쓰는 도구"로 넘어가지 못한 상태.

## 01. 유지할 것

- **데이터와 코드의 완전한 분리** — `data/`의 JSON만 교체하면 되고, `validate:data`가 배포 게이트 역할.
- **오프라인 전략** — 셸/자산/이미지 캐시 분리, 이미지 개수 상한(160), 저장한 논문의 원문 페이지 선캐시.
- **Pretendard 서브셋 self-host** — 2MB → 456KB.
- **내 일정의 시간 충돌 감지 + .ics 내보내기** — 이 앱의 가장 큰 차별점.
- **공지사항 구글 시트 런타임 연동 + 캐시 폴백** — 백엔드 없이 재배포 없는 운영.
- **초록 추출 품질 점수와 검수 경고** — `extractionQuality` 0.55 미만 시 안내 문구 노출.

## 02. 배포 전 결함

### P0
1. **더보기 메뉴 글자가 붙어서 출력** — `app/globals.css`의 `.more-menu div div{display:flex;flex-direction:column}`이
   실제 DOM(`.more-menu > a > div`)과 맞지 않아 규칙이 전혀 적용되지 않음.
2. **다크 모드에서 헤더 제목이 사라짐** — 히어로 배경 사진은 고정인데 텍스트만 다크 토큰으로 전환되어 대비 실패.
3. **초록 전문이 모든 화면의 번들에 포함** — `lib/conference.ts`가 `papers-with-abstracts.json`을 통째로 import해
   공지사항·더보기 등에서도 gzip 244KB가 함께 로드됨.
4. **배치도가 춘계 행사장(쏠비치 삼척) 자산** — 설정은 "여수 엑스포컨벤션센터"인데 도면은 다른 장소.

### P1
5. 별표 터치 타깃 30×30px (권장 44×44 미달)
6. 화면마다 헤더 색이 다름 (탭 화면 흰 배경 vs 상세 화면 진한 초록)
7. 한국어 UI에 영어 혼용 ("Open original page", 영문 아이브로우 8종)
8. 어순이 어긋난 문구 ("4 새 소식" → "새 소식 4건")

### P2
9. 논문 상세의 픽셀 크기 등 개발자용 정보 노출
10. 아이콘 체계 밖 리터럴 글리프(⌖) 사용
11. 죽은 CSS 선언 `--card-2:#1b2apx`
12. 브랜드 초록 3종 혼재 (`#00796b` / `#006b5b` / `#004d43`)

## 03. 디자인 진단

- **D-1** 카드마다 같은 말을 두 번 함 — 뱃지와 제목이 사실상 동일 문자열.
- **D-2** 영문 아이브로우(DISCOVER, NOW & NEXT 등) 9종이 순수 장식으로 화면마다 40~50px 차지.
- **D-3** 8개 홀 동시진행 세션을 세로 목록으로 나열 — 한 타임슬롯이 2,600px. 시간×홀 그리드 뷰로 전환 필요.
- **D-4** 논문 319편 필터에 정작 34개 학술 분야(주제) 축이 없음.
- **D-5** 헤더가 화면마다 165~300px 차지 — 홈 이외엔 48px 고정 헤더로.
- **D-6** 초록 본문 타이포가 목록 카드와 동일 크기 — 읽기 전용 스케일 필요.
- **D-7** 키워드 칩이 링크가 아님 — 탐색 진입점으로 활용 안 됨.
- **D-8** 날짜 스트립이 177×100px 카드 4장 — 화면 낭비.
- **D-9** 상세 화면에 이전/다음 이동 경로 없음.
- **D-10** 별(★) 아이콘이 "내 일정"과 "후원사"에 중복 사용.

## 04. 화면 구성 제안

하단 탭 4개(프로그램/논문/내 일정/더보기) 구조는 유지하되 역할을 명확히 분리:

- **프로그램 = 시간축** — "지금·오늘 어디로 가야 하나"에만 답한다.
- **논문 = 주제축** — "내 분야에 뭐가 나왔나"에만 답한다.
- **내 일정 = 개인화** — 이미 강점. 알림·메모로 확장.
- **더보기** — 현장/대회/앱 정보로 그룹화.

검색 진입점(헤더 돋보기 vs 논문 탭 검색창)도 로직이 중복 구현되어 있어 통합 필요.

## 05. 벤치마킹

| 레퍼런스 | 배울 점 | 이 앱의 상태 |
|---|---|---|
| Sched | 그리드/리스트 토글, 트랙을 열로 세운 격자 뷰 | 없음 — 최우선 도입 대상 |
| Whova | 참가자 프로필, Q&A, 실시간 투표 | 범위 밖 (구글폼으로 일부 대체 가능) |
| EventsAir | 세션 평가, 전자포스터 | 부분 구현 (원문 뷰어 有) |
| Cvent | QR 명찰·체크인 | 범위 밖 |
| Morressier/Oxford Abstracts | 키워드 색인, 유사 연구 추천 | 부분 (데이터는 있음, 색인 없음) |
| ACM/IEEE 학회 웹앱 | PDF 딥링크, 오프라인 우선 | 근접 — 오프라인은 오히려 앞섬 |
| 국내 학회 앱 일반 | 프로그램북 PDF 뷰어 수준 | 이미 우위 |

결론: 백엔드 없이 트랙 그리드 뷰·주제 필터·키워드 색인 세 가지만 갖춰도 국제 기준에 근접한다.

## 06. 로드맵

**1단계 (약 1주) — 결함 제거**
더보기 메뉴 CSS 수정, 다크 모드 헤더 대비, 번들 분리(초록 서버 컴포넌트 격리),
터치 타깃 44×44, 한글화, 브랜드 색 통일, validate:data에 장소 일치 검사 추가.

**2단계 (약 2~3주) — 정보 설계 재작업**
세션 카드 재설계, 시간×홀 그리드 뷰, 주제 필터(34개 분야), 영문 아이브로우 삭제,
헤더 통일, 키워드 링크화 + 읽기용 타이포, 검색 통합, 상세 화면 이동 경로.

**3단계 (약 4주) — 현장에서 쓰이게**
발표 10분 전 알림, 키워드 역색인, 포스터 보드 맵, 세션 메모, 세션 평가(시트 연동),
영문 UI, 실데이터 리허설.

지금 당장 한 가지만 한다면 — **"시간대 × 홀 그리드 뷰"**가 체감 효과가 가장 크다.
데이터는 이미 다 있고, 새로 만들 컴포넌트는 하나이며, 학술대회 앱을 쓰는 이유의 절반을
이 화면이 담당한다.
